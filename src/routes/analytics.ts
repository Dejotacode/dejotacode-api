import { Hono } from 'hono'; import { z } from 'zod'; import type { AppEnv } from '../types'; import { fail,ok } from '../lib/response'; import { requireAuth } from '../middleware/auth'; import { hashToken } from '../lib/crypto';
const eventSchema=z.object({event:z.enum(['page_view','cta_click','lead_submit','contact_submit']),path:z.string().startsWith('/').max(240),campaign:z.string().max(100).optional()});
export const analytics=new Hono<AppEnv>();analytics.post('/',async(c)=>{const address=c.req.header('cf-connecting-ip')??'local';const key=await hashToken(`analytics:${address}`);const limited=await c.env.ANALYTICS_RATE_LIMITER.limit({key});if(!limited.success)return fail(c,'RATE_LIMITED','Limite de métricas atingido.',429);const parsed=eventSchema.safeParse(await c.req.json());if(!parsed.success)return fail(c,'VALIDATION_ERROR','Evento inválido.',400);const date=new Date().toISOString().slice(0,10);await c.env.DB.prepare("INSERT INTO daily_metrics (metric_date,event_type,path,campaign,total) VALUES (?,?,?,?,1) ON CONFLICT(metric_date,event_type,path,campaign) DO UPDATE SET total=total+1,updated_at=CURRENT_TIMESTAMP").bind(date,parsed.data.event,parsed.data.path,parsed.data.campaign??'').run();return ok(c,{accepted:true},201);});analytics.get('/summary',requireAuth,async(c)=>{const totals=await c.env.DB.prepare("SELECT event_type AS event,SUM(total) AS total FROM daily_metrics WHERE metric_date>=date('now','-30 days') GROUP BY event_type").all();const pages=await c.env.DB.prepare("SELECT path,SUM(total) AS total FROM daily_metrics WHERE event_type='page_view' AND metric_date>=date('now','-30 days') GROUP BY path ORDER BY total DESC LIMIT 10").all();return ok(c,{periodDays:30,totals:totals.results,pages:pages.results});});
