{
  "name": "@dejotacode/api",
  "version": "1.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply DB --local",
    "db:preview": "wrangler d1 migrations apply DB --env preview --remote",
    "db:production": "wrangler d1 migrations apply DB --env production --remote",
    "backup:d1:production": "node scripts/backup-d1-production.mjs",
    "inventory:r2:production": "node scripts/inventory-r2-production.mjs",
    "check:ops": "node --check scripts/backup-d1-production.mjs && node --check scripts/inventory-r2-production.mjs && npm run backup:d1:production -- --dry-run"
  },
  "dependencies": {
    "hono": "^4.13.7",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.132.0"
  }
}
